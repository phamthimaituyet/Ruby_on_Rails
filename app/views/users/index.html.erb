  <nav class="navbar navbar-default">
    <div class="container-fluid">

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <%# quay ve trang home %>
          <li role="presentation"><%= link_to "Home", '/', :method => :get %></li> 
          <li role="presentation"><%= link_to "Post", posts_path %></li>
          <li role="presentation"><%= link_to "Profile", users_path %></li>
        </ul>
        <form class="navbar-form navbar-left">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>

        <div class = "thongtincanhan">
          <% if user_signed_in? %>
              <button type="button" class="btn btn-default navbar-btn"><%= link_to "Đăng xuất", destroy_user_session_path, method: :delete %></button>
            <% else %>
              <button type="button" class="btn btn-default navbar-btn"><%= link_to "Đăng nhập", new_user_session_path %></button>
              <button type="button" class="btn btn-default navbar-btn"><%= link_to "Đăng ký", new_user_registration_path %></button>
            <% end %>
        </div>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  
  
  <p id="notice"><%= notice %></p>
 
<div class="container">
  <% if user_signed_in? %>
    <div class = "container_user">
           <div class = "avatar">
             <% if @user.avatar.attached? %>
              <%= image_tag @user.avatar %>
            <% end %>
          </div>
          <h3><%= @user.email %></h3>
          <h3><%= @user.name %></h3>
          <button type="button" class="btn btn-default navbar-btn"> <%= link_to 'Edit', edit_user_path(@user) %></button>
          <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' } %></button>
  </div>
  <% end %>

 <% @posts.each do |post| %>
 <div class = "container_post">
  <div class="well">
        <h3><%= link_to post.title, post %></h3>
        <p><%= post.content %></p>
        <div class = "image">
          <% if post.image.attached? %>
             <%= image_tag post.image %>
          <% end %>

        </div>
          <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Show', post %></button>
          <% if user_signed_in? %>
            <% if post.user_id == current_user.id %>
              <button type="button" class="btn btn-default navbar-btn"> <%= link_to 'Edit', edit_post_path(post) %></button>
              <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></button>
            <% end %>
          <% end %>

  </div>
</div>
 <% end %>
</div>