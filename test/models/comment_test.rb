require "test_helper"

class CommentTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true               : kiểm tra thuộc tính này có giá trị như thế kia không ? 
                                  # Kiểm tra đối tượng có phải nil hay không ?
                                  # Kiểm tra đoạn code giải phóng exception hay không? 
  # end
end
